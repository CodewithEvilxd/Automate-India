name: circularchain_mobile
description: "CircularChain Mobile — AI-Verified Circular Economy Industrial Ledger App"
publish_to: 'none'
version: 2.6.0+26

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  image_picker: ^1.0.7
  provider: ^6.1.1
  qr_flutter: ^4.1.0
  shared_preferences: ^2.2.2
  url_launcher: ^6.2.4
  google_fonts: ^6.1.0
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
